## ResponseBodyIssue: Java library of standardized HTTP response bodies.
***
[![license](https://img.shields.io/badge/license-MIT-blue)](LICENSE)

Read in another language: [ru](./localization/ru/README.md)

## Description
***

The ResponseBodyIssue Java library contains DTO classes 
for HTTP 4xx responses. The library provides a unified response-body format
for microservice-based server architectures. 
It allows services to expose a consistent error contract
without exposing internal implementation details, such as 
service classes, exceptions, internal architecture, or underlying frameworks.

## Table of contents
***

- [Getting Started](#getting-started)
- [Usage](#usage)
- [Internal Structure](#internal-structure)
- [License](#license)

## Getting Started
***

To build the project and install it into the local Maven repository, 
run the following command from the project's root directory:

```bash
mvn clean install
```

To add ResponseBodyIssue as a Maven dependency, 
add the following code to your project's `pom.xml` file:

```xml
<dependency>
    <groupId>ru.reel</groupId>  
    <artifactId>response-body-issue</artifactId>
    <version>1.1</version>
</dependency>
```

## Usage
***

1. Simple example of creating a `RequestError` entity without message parameters:
```java
import ru.reel.request.error.RequestError;
import ru.reel.request.error.reason.ErrorReason;

RequestError.builder()
            .errorReason(ErrorReason.JSON_FORMAT)
            .message()
            .build();
```

The resulting object in JSON format:

```json
{
  "errorReason" : "JSON_FORMAT",
  "message" : "Invalid JSON format."
}
```

2. Example of creating a `RequestError` entity 
with one parameter for the standardized message generated by `ErrorMessageFactory`:
```java
import ru.reel.request.error.RequestError;
import ru.reel.request.error.reason.ErrorReason;

RequestError.builder()
            .errorReason(ErrorReason.EMPTY)
            .message("field_name") // %s is empty or null.
            .build();
```

The resulting JSON object:

```json
{
  "errorReason" : "EMPTY",
  "message" : "field_name is empty or null."
}
```

3. Example of creating a `RequestFieldError` entity
   with a few parameters for the standardized message generated by `ErrorMessageFactory`:
```java
import ru.reel.request.error.RequestFieldError;
import ru.reel.request.error.reason.ErrorReason;

RequestFieldError.builder()
            .field("password")
            .errorReason(ErrorReason.LESS_SIZE)
            .message("password", "6") // %s cannot be less than %s.
            .build();
```

The resulting JSON object:

```json
{
  "field" : "password",
  "errorReason" : "LESS_SIZE",
  "message" : "password cannot be less than 6."
}
```

4. Example of creating a `RequestFieldError` entity
   with a custom message:
```java
import ru.reel.request.error.RequestFieldError;
import ru.reel.request.error.reason.ErrorReason;

RequestFieldError.builder()
            .field("password")
            .errorReason(ErrorReason.LESS_SIZE)
            .customMessage("Password cannot be less than 6 characters!")
            .build();
```

The resulting JSON object:

```json
{
  "field" : "password",
  "errorReason" : "LESS_SIZE",
  "message" : "Password cannot be less than 6 characters!"
}
```

To determine the number of parameters required for a standardized message,
refer to the JavaDoc documentation for the `ErrorMessageFactory` class.

## Internal Structure
***

- **Issue** - contains the abstract base Issue DTO.
- **Error** - contains DTOs for 4xx responses.
    - **Reason** - contains the enum defining 4xx response reasons.
    - **Message** - contains the factory for standardized error messages.

## License
***

This project is licensed under the terms of the MIT license.